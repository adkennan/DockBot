EXECUTE /VERSION

CD //

SET FN Dockbot-`getenv $DOCKBOT_VERSION`.lha

IF EXISTS $FN
  DELETE $FN
ENDIF

IF NOT EXISTS DockBot.info
  COPY DockBot/src.Info DockBot.info
ENDIF

LHA a -r $FN @DockBot/filelist

CD DockBot/src
